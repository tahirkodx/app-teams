// import getConfig from "next/config";
// const { publicRuntimeConfig } = getConfig();

// const CONFIG = {
//   BASE_URL: publicRuntimeConfig.BASE_URL,
// };

// export default CONFIG;

const CONFIG = {
  BASE_URL: "https://dev.api.epicus.ai",
};
export default CONFIG;
